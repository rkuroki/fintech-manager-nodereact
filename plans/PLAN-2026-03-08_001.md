# Implementation Plan — 2026-03-08_001

## Overview
Three major tasks for the Investor Management Backoffice Platform:
1. Docker SQLite persistence + DBeaver access
2. User Group ↔ Squad Role model (access control restructuring)
3. Customer detail page (full rewrite with timeline UI)

## Task Files
- [TASK-001: Docker SQLite Persistence](tasks/PLAN-2026-03-08_001_TASK-001_docker-sqlite-persistence.md)
- [TASK-002: Group Squad Role Model](tasks/PLAN-2026-03-08_001_TASK-002_group-squad-role-model.md)
- [TASK-003: Customer Detail Page](tasks/PLAN-2026-03-08_001_TASK-003_customer-detail-page.md)

## Execution Order

### Phase 1: Docker & Infrastructure (quick wins)
1. Create `data/.gitkeep`, update `.gitignore`
2. Verify docker-compose volume persistence works correctly

### Phase 2: Database Schema Changes
3. Create migration `0002_groups_customers_roles.sql`
4. Update Drizzle schema files (`users.ts`, `customers.ts`)
5. Update shared types and schemas

### Phase 3: Backend — Group Squad Roles
6. Update group CRUD to handle mnemonic + auto-create squad role
7. Add manager/analyst group validation
8. Update seed data with groups, squad roles, customer role assignments

### Phase 4: Backend — Customer Roles & APIs
9. Add `customer_access_roles` repository functions
10. Add customer roles endpoints (`GET/PUT /customers/:id/roles`)
11. Add `GET /customers/by-mnemonic/:mnemonic` endpoint
12. Add communication update/delete endpoints
13. Add document download endpoint
14. Update `createCustomer` to auto-assign squad role

### Phase 5: Frontend — Customer Detail Page
15. Create new `CustomerPage.tsx` with section layout
16. Build `BasicInfoSection` and `SensitiveInfoSection`
17. Build `NotesFilesTimeline` with sidebar drawers
18. Build `CommunicationsTimeline` with sidebar drawers
19. Build `AccessRolesSection` (manager-only editing)
20. Update routes and navigation links
21. Keep `CustomerFormDrawer` for quick-create from list page

### Phase 6: Verification
22. Test Docker persistence (stop → start → data still there)
23. Test DBeaver connection to `./data/dev.db`
24. Test group creation with squad role auto-creation
25. Test customer creation with auto role assignment
26. Test customer detail page — all sections, edit, timeline CRUD
27. Test access roles — manager can edit, analyst cannot
